# Independent release and data review · 2026-09-07

> Historical intermediate review. Lead follow-up: the renderer failures below were fixed and independently reviewed. Final local source passes 154 tests with one Windows symlink skip; the explicit runtime roster now has 44 files, and final GUI/AI/import review and actual Codex downloads pass. This note does not revise the original critic's observations. Exact-commit CI, live checks and the remaining Gmail boundary are recorded in the current gate table in [PROGRESS.md](../../PROGRESS.md).

**Verdict: HOLD.** The latest full run during the active renderer revision has three renderer failures. A successful build is not sufficient to release that state. Exact-commit CI and Pages evidence, current visual approval, native download confirmation, and the selected Gmail received-message check also remain unclosed in this review.

This critic owns `tests/studio-integration.test.mjs` and this report only. It inspected actual application handlers, session codec, renderer, PNG exporter, public build/server, workflow, bundled detector files and prior progress evidence. It did not read the user's private backup, control a browser, send email, change the app, commit or push. The source remains an uncommitted work in progress based on `3c46e99`; earlier release evidence does not certify these changes.

## Observed test state

The first independent full run reported **104 pass / 2 fail / 1 Windows symlink skip** (107 tests). Both failures were real pending-photo regressions: changing shape while loading discarded the selected photo, and changing size during detection discarded the smart crop. The lead's fixes have been independently rerun in the full suite and both now pass.

The latest independent full run reported **112 pass / 3 fail / 1 Windows symlink skip** (116 tests). The newly failing tests are in `tests/design-rendering.test.mjs` while the renderer builder is revising compositions:

- `all seven compositions support minimum, default and maximum dimensions in both exports`: rendering raises a height-fit error.
- `all designs preserve optional contacts, selected icons, links and user colors`: rendering raises a height-fit error.
- `name and field fit failures stop every design from silently overflowing`: the title-fit assertion receives no error at line 91.

The new application tests for design/history, photo export/reset/restore, uploaded-photo HTML/share blocking, and normal rich clipboard all pass in that run. All 13 portrait tests and all five tests owned by this critic pass. `npm run build` succeeds with **34 files**. `git diff --check` reports no whitespace errors. This records the observed state and must be replaced by a new full result after the renderer stabilizes.

## Independent data evidence

`tests/studio-integration.test.mjs` uses a synthetic, independently hardcoded 23-field pre-studio draft; it does not construct the migration oracle from the current defaults. Its Unicode name, every contact, selected icon, colors, dimensions, layout, asset URL, saved theme provenance, selected theme and view/export settings survive parsing and a second serialized round trip. The six new defaults are checked explicitly: original design, automatic pattern, empty photo data/URL, circle shape, and 64-pixel size.

Every explicit expected design ID — Original, Orbit, Studio, Contour, Prism, Editorial and Signal — round-trips with an independent Contour pattern, a synthetic PNG data URL, hosted photo URL, 96-pixel rounded shape, all old details and an existing saved theme. Reimport preserves the selected theme without duplicating it. Preview retains the local bytes; email markup uses the hosted URL and contains no inline image data.

A separate path restores data-only photos for every named design. Session export succeeds, preview/PNG markup includes the photo, and plain text remains meaningful without base64 data. Email HTML raises an explicit public-HTTPS-photo error. The actual app harness also verifies that Copy signature, Download HTML and Share draft cannot silently export a broken email photo or a huge inline-photo link. These are actual JavaScript handler tests in a Node fixture, not native browser API results.

One small application issue was sent to the lead: after a browser-storage failure, `save()` still advised Download HTML even though a local-only photo intentionally blocks that export. The recovery instruction should direct the user to Export data, preserving photo bytes and the rest of the editable session.

## Public delivery boundary

The independently fixed expected public roster includes both editor entry points, core/session/history/image modules, all four portrait runtime files, all six new patterns, prior icons/artwork, and the Pico runtime, model and MIT license. The new test copies the actual source bytes into an isolated scratch source, builds it using the production builder, and checks every output byte and the complete file list. No `.private` files, test photos, Git data or review documents enter the site build. Entry points remain byte-identical and their local JS/CSS dependencies are present.

The production HTTP server is exercised with real files. Worker/runtime/model/license routes return the expected MIME type and exact bytes. Test-fixture, artwork-documentation, review and private-session routes return 404. Existing release tests separately exercise raw HTTP encoded traversal, stale build cleanup, failure before overwriting the prior good build, and symbolic links. The Windows account cannot create the symlink fixture; the Linux CI run must execute that check.

The cascade is 239,632 bytes with SHA-256 `d8014993e7298c7b1865d1f8b855d6dbf4ec5c808bf879e2091ab6837abf90cd`; Pico JavaScript SHA-256 is `785b981cc79e5fa3f7557dc3fa7773629d7529994d7627de41b77d8687649309`. Both match the recorded vendor source. The published license includes the author's copyright, permission-retention clause and warranty text. The author marks [Pico JavaScript as MIT](https://github.com/nenadmarkus/picojs); the bundled full text matches the [Pico repository license](https://raw.githubusercontent.com/nenadmarkus/pico/master/LICENSE). The portrait worker fetches its model from this site's local `vendor/` route. No photo upload service appears in the inspected portrait code.

The allowlist oracle was tested against a real mutation in a temporary source copy: removing `vendor/PICO-LICENSE.txt` from the production allowlist made the actual new public-build regression fail with test-runner exit 1. This proves an omitted required runtime/legal file cannot disappear from both the implementation and its own test expectation. The temporary copy was removed after checking its resolved path remained inside `.test-tmp`.

The site allowlist covers deployment, not repository history. The prior progress record already discloses that old public commits contain personal details; this critic did not rewrite or certify that history. The original Downloads checkout and private backups were not read or altered.

## Required remaining evidence

| Gate | Current review status | Required closure |
| --- | --- | --- |
| Full local regressions | Failed in active renderer revision | Stable final source, all checks pass; record count and exact revision |
| Independent visual comparison | Pending other critic's final revision | Actual matched reference/artifact captures and approval after composition changes |
| Exact-commit GitHub workflow | Pending | Final commit's build/test/upload/deploy jobs execute and succeed; inspect Linux symlink check |
| Published Pages parity and use | Pending | Published files match reviewed source; critical new design/photo/export controls exercised |
| Native JSON/PNG saving | Unproven | Actual browser download produces a file; Blob and click assertions alone do not close it |
| Hosted personal-session restoration | Pending lead's safeguarded browser work | Preserve existing session first, then prove imported private data survives without publishing it |
| Real Gmail received signature | Blocked in progress record | Authenticated Gmail and authorized test send, then received desktop/mobile/light/dark inspection |
| Portable offline app | Not selected | No offline-app claim; a development source ZIP is a different artifact |

The existing `.github/workflows/pages.yml` runs `npm test` and `npm run build` on main push/PR/manual dispatch. Its test coverage includes Node-controlled application events and local HTTP, not an automated real browser or authenticated Gmail. Main non-PR builds upload `dist` and deploy Pages. A workflow file and a green older commit do not close the new exact-commit gate. A final independent re-review is required after the lead supplies the final commit and live evidence.
