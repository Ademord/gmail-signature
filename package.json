{
  "name": "gmail-signature-editor",
  "version": "1.0.0",
  "private": true,
  "description": "A browser-local HTML email signature editor with static hosting.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
